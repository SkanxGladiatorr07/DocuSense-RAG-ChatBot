# ── API ───────────────────────────────────────────────────────────────────────
# Base URL for the Express backend API.
# In development, Vite proxies /api calls to localhost:5000 automatically.
# Only change this if your backend runs on a different port or in production.
VITE_API_BASE_URL=/api/v1
